@model IEnumerable<Categories>
@foreach(var item in Model)
{
	<div class="panel panel-default">
		<div class="panel-heading">
			<h4 class="panel-title"><a asp-controller="Category" asp-action="Category" asp-route-CategoryId="@item.CategoryId">@item.CategoryName</a></h4>
		</div>
	</div>
}